---
description: Mandatory verification gates for behavior changes in ff-player
globs:
  - "src/**"
  - ".github/workflows/**"
  - "package.json"
alwaysApply: true
---

# Testing and Verification Rule

## 1) Required sequence
1. Isolate behavior in small testable units first.
2. Add or update unit tests around the changed behavior when the touched path has a practical harness.
3. Implement wiring after edge cases and invariants are clear.
4. Run repo verification before calling the task done.

## 2) Verification expectations
- Required repo-wide verification:
  - `npm run lint`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
- Current unit-test baseline:
  - Vitest is available for fast unit tests against pure helpers and small service logic.
  - Prefer starting with deterministic utilities, parser/normalizer code, scheduling math, and command handlers before browser-heavy integration paths.
- If a change affects command handling, scheduling, persistence, or media orchestration and a focused unit test can be added without standing up the whole app, add it.
- When the repo still lacks a practical automated harness for a changed path, do not claim coverage that does not exist; call the gap out explicitly.

## 3) Constraints
- Do not ship behavior changes without automated verification when the change can reasonably be covered.
- Do not rely on CI comments or autofixers as proof of quality.
- Do not skip build verification for changes that affect routes, runtime config, model shapes, or app bootstrapping.

## 4) CI alignment
- If a required local verification step exists, CI should enforce it where practical.
- When adding new guardrails, prefer making them blocking checks instead of advisory comments only.
